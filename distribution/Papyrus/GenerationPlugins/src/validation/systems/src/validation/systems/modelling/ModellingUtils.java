package validation.systems.modelling;

/**
 * This class offers a set of utils for the updating of the notation model files
 * 
 * @author fernand0labra
 *
 */
public class ModellingUtils {

	//=================================================================================================
	// methods
	
	//-------------------------------------------------------------------------------------------------
	/**
	 * Add the notation for a new reference in the UML model
	 * 
	 * @param identifier The new identifier of the object
	 */
	public static void addNotation(String identifier) {
		// TODO Complete
		
		//		DocumentBuilderFactory dbf = DocumentBuilderFactory.newInstance(); 
		//		DocumentBuilder db = dbf.newDocumentBuilder(); 
		//		Document doc = db.parse(new File(""));  
		//
		//		doc.getDocumentElement().normalize(); 
		//		Element root = doc.getDocumentElement();
	}
	
}
